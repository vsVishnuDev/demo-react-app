name: Deploying a web site
on:
  push:
    branches:
      - master
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Printing a message
        run: echo "Hi there!"
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Printing another message
        run: echo "Good bye!"
